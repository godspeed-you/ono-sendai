[package]
name = "ono-kuang-sdk"
description = "What a KUANG/11 plugin author uses: declare the manifest surface, answer invocations and queries, stream values, request capabilities (spec §31.59)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
tar.workspace = true
ono-kuang-protocol.workspace = true
ono-value.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
bytes.workspace = true
# The change vocabulary the conformance suite composes risk with: §19.2's maximum is
# `RiskAssessment::classify`, and a test that re-implemented it would prove nothing about it.
ono-change-core.workspace = true
# `protocol.v1.yaml` is the contract the dispatch is compared against, in both directions.
serde_yaml_ng.workspace = true
ono-value.workspace = true
ono-testkit.workspace = true
async-trait.workspace = true
ono-model-broker.workspace = true
libc.workspace = true
ono-kuang-supervisor.workspace = true
ono-kuang-testhost.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["macros", "rt"] }

[lints]
workspace = true
