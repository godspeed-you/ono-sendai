# Ono-Sendai v0.6.2 — the verification foundation

A foundation release on top of v0.6.1. It changes what the shell is built from and how it is
proven, not what it does: the binary is 22 MB instead of 46, a budget keeps it there, KUANG/11
components are compiled once at install instead of at every load, a static core build exists for
containers and embedded Linux, and the test suite, the acceptance harness and the release tooling
give the same answer on a loaded machine as on a quiet one. The GitHub milestone `v0.6.2`
("Verification Foundation") is the release inventory. It has no narrative specification, and
ADR-0862 records that each of its 24 issues is a requirement whose exit test decides it.

> Establish a reliable test, build and release foundation before making larger semantic changes.

## A smaller shell, and a budget that keeps it small

The release profile optimises for size: `opt-level = "s"` with fat LTO, still unwinding on panic
(#124). The Cranelift compiler left the shell (#126, below). Together they take the stripped
`x86_64` binary from **46,094,688 to 22,156,576 bytes**. On one tree, on a quiet machine, the new
profile is 1–6 % slower in CPU-bound first rows (`spatial.look` cold, `process.enumeration`),
equal in cold start (p95 5.6–6.4 ms against 6.0–6.3 ms), and holds every §34 target.

The stripped size is now a measured figure with a budget beside the other limits
(`docs/contracts/hardening/limits.yaml` → `build_budgets`): 24,350,304 bytes for `ono`, the
integrated figure plus ten percent, and under 8,000,000 for the core build. `cargo xtask
binary-size` checks them, the gate runs it when a current release build exists, and CI's image
build requires it, so the next 15 MB arrive as a red push rather than as a surprise in a package.
`xtask metrics` records the figures per binary and target in `docs/baselines/binary-size.yaml`
and the README, and `build-manifest` carries them into the release input manifest (#125,
ADR-0864).

The seven minutes of CI's release build are a cold dependency build plus a serial tail: the shell
at one codegen unit, then the LTO link. The packaging job now publishes cargo's timing report for
every push, and the release tool, which is not shipped, builds at its own profile (#218, ADR-0865).

## KUANG/11 components are compiled once, at install

`ono` no longer links a compiler. `install plugin` runs the SDK's new `kuang-compile` on a
component package once, which writes the engine's native form into a store only its owner writes;
the shell maps that artifact and refuses a missing, stale, foreign or untrusted one with
`load.component_not_compiled` (Ono-Sendai-K11105), naming the exact `kuang-compile` command to run.
An artifact is compiled for the architecture, not for the machine, so an image or a system store
built on one host loads on every other of its kind. Both packages ship `/usr/bin/kuang-compile`
beside `/usr/bin/ono`. (#126, ADR-0870, ADR-0905, ADR-0914; `docs/MIGRATION.md` §10)

## A core build for containers and embedded Linux

`cargo build --release -p ono-cli --no-default-features --features core --target
x86_64-unknown-linux-musl` builds the object shell alone — the language, the evaluator, typed
pipelines, the native commands, the Linux and network providers — as one static binary of
7,063,744 bytes. A command of a tier it leaves out refuses with `resolve.not_in_build`
(Ono-Sendai-E0104); `get service` refuses with the same `provider.unavailable` a full build gives
on a host without systemd; `help` and completion show only what the build carries, and `ono
--version` names the profile. `scripts/build-core.sh` builds and checks it, 27 acceptance cases
run against it in a `FROM scratch` image, and CI does both on every push. The default build is
the full product, unchanged. (#127, ADR-0910 … ADR-0913)

## Tests that hold on a busy machine

- A test that compared two readings of the live host now reads it once, owns the fixture it
  reads, or brackets the second reading: `near` (#165), the service count behind a bounded
  orientation, now against a service manager the test runs itself (#155), the mount table, the
  type-filtered map and the completion that waited for an echo instead of a place (#215), and the
  interface trace whose graph can outgrow its node limit on a busy host (ADR-0880).
- Fixed sleeps and wall-clock bounds became observations: a background job is waited for, not
  slept on (#164); a file event is judged by where it came from, not by how long the machine took
  (#160).
- A budget that measures the machine says which machine it is measured on: the Profile L live map
  (#166, ADR-0881) and case 152's pathological sockets, read against the same machine in the same
  run (#189, ADR-0882).
- The two `change_claims.rs` tests reported at 31 s take under a second; the FIFO hypothesis was
  refuted by timing and `strace` (#141).
- The suite leaves nothing behind. Scratch space lives in cargo's `target/tmp` (#143, ADR-0890);
  a test script or program is written by a process of its own, so `exec` never meets `ETXTBSY`
  (#188, ADR-0891); an overrunning or failing test ends the whole process tree it started and
  reaps it, and the journal-follower test asserts the follower's death (#162, #204, ADR-0892).

## Release tooling that cannot compare against yesterday

- `release-check.sh` builds into a directory it owns, and `package.sh` refuses to write beside
  another version's packages (#145).
- The RPM is byte-reproducible: every member carries the epoch, whatever age its source file has
  (#146, ADR-0902).
- `xtask perf` refuses a row sampled by a build other than the one it claims, reports a figure
  from another build as uncomparable, and reports a run under load as not adjudicated rather than
  as a regression (#151, #169, ADR-0904, ADR-0908).
- Each checkout builds its acceptance image, and package validation its test image, under a tag of
  its own, and neither removes an image another run still uses (#185, ADR-0901, ADR-0907).
- CI keeps the acceptance image's filesystem stage in a buildx layer cache instead of downloading
  it from the Ubuntu archive on every run; a cache miss still builds (#139, ADR-0903).
- `THIRD-PARTY-LICENSES` owes exactly what the shipped binaries link, each resolved on its own
  (ADR-0906), and AGENTS.md names the sub-branch form git can create, which CI now builds (#196,
  ADR-0900).

Found and fixed on the way: `kuang-sign pack` wrote a file with holes as a GNU sparse entry the
host refuses to unpack; the gate did not select the packaging suite when `THIRD-PARTY-LICENSES`
moved; four tests read rendered output whose width depended on the checkout's depth.

## Compatibility

Nothing a v0.6.1 configuration or script relies on changes. A component package installed before
v0.6.2 has no compiled artifact yet: its first load refuses with the `kuang-compile` command to
run, once (`docs/MIGRATION.md` §10). A shell built from source needs `kuang-compile` beside it to
install component packages (README, *Installing it*).

## Traceability

Every issue of the milestone, what closes it, and what proves it. Commits are on `implementation`;
"gate" is `scripts/gate.sh` with every package, "CI" the push workflow, "case" an acceptance case.

| Issue | Requirement (exit test) | Implementation | Proof | Result |
|---|---|---|---|---|
| #124 | release profile `opt-level = "s"`, fat LTO; §34 targets held; `verify-release.sh` passes on the package | `Cargo.toml` `[profile.release]`; ADR-0863 | stripped size 46,094,688 → 22,156,576 B; interleaved A/B `xtask perf --profile S` at load < 3, every §34 target held; `package.sh` + `package-check.sh` + `verify-release.sh --without-signature` green | met |
| #125 | stripped size in `metrics`, README and `build-manifest`; budget in the limits registry; gate fails over budget | `xtask/src/metrics.rs`, `binary-size`, `provenance.rs`, `limits.yaml` `build_budgets`, `docs/baselines/binary-size.yaml`, `scripts/gate.sh`, `docker/Dockerfile`; ADR-0864 | `xtask/tests/metrics.rs` (15 tests), `provenance.rs::should_carry_the_recorded_stripped_binary_size_and_its_budget_into_the_manifest`; CI image build runs `binary-size --require` | met |
| #126 | `ono` without `cranelift`/`parallel-compilation`; component cases pass unchanged; stale or missing artifact refused naming the step | `ono-kuang-supervisor` `compiled.rs`, `compiler` feature, `kuang-compile`, install hook, K11105 contract, Dockerfile, packages; ADR-0870, 0905, 0914 | `ono-kuang-sdk/tests/compiled.rs` (11), `compiled::portable` (baseline CPU), `plugins.rs`; cases 214, 350–352; `cargo tree -i cranelift-codegen` empty for `ono`; packaged `ono` has no `cranelift_codegen` | met |
| #127 | static musl core binary < 8 MB; core cases pass in `FROM scratch`; `get service` refuses as on a host without systemd | `ono-cli` `[features]`, `src/absent/`, `CommandRegistry::retaining`, `ono-provider-linux` `systemd` feature, `scripts/build-core.sh`, `docker/core/Dockerfile`, `acceptance.sh --profile core`, CI `core-build`; ADR-0910 … 0913 | 7,063,744 B static-pie; `crates/ono-cli/tests/core_build.rs` (9); `--profile core` 27/27; cases 355–359; CI job `core-build` | met |
| #139 | the filesystem stage is not downloaded on every CI run | buildx `type=gha` layer cache in `ci.yml`, `acceptance.sh` `ONO_ACCEPTANCE_LAYER_CACHE`; ADR-0903 | `xtask/tests/harness.rs`; CI run 35993693134 built the filesystem image in 22 s from the cache the previous run exported | met |
| #141 | explain or fix the 31 s of two `change_claims.rs` tests | none needed; the children are owned since #162 | timed: both tests 0.47 s in the integrated gate; `strace`: each apply opens the FIFO once | not reproducible; closed with evidence |
| #143 | `scratch()` does not land in `/tmp` outside a test's own crate | `ono-testkit` `scratch.rs` run-time target discovery; ADR-0890 | `ono-recovery-files/tests/store.rs::should_give_a_suite_scratch_space_on_the_filesystem_cargo_builds_into` | met |
| #145 | release-check builds into a directory it owns | `release-check.sh`, `package.sh`, `package-check.sh --dist` | `packaging.rs::should_build_each_release_check_into_a_directory_it_owns`, `::should_refuse_to_package_beside_the_artifacts_of_another_build` | met |
| #146 | two clean builds give byte-identical RPMs | `package.sh` epoch-stamped staging, `rebuild-check.sh`; ADR-0902 | `packaging.rs::should_produce_identical_hashes_for_two_clean_builds_of_one_commit` with source mtimes that differ | met |
| #151 | a debug figure is never labelled release | `perf::SAMPLER_BUILD`, `Comparison::ForeignBuild`; ADR-0908 | `perf.rs::should_refuse_a_temporal_row_sampled_by_a_build_other_than_the_one_the_run_claims`, `::should_report_a_figure_from_another_build_as_uncomparable_rather_than_as_a_regression` | met |
| #155 | the bounded orientation count comes from one reading or an owned fixture | private `dbus-daemon` serving `org.freedesktop.systemd1`; ADR-0880 | `spatial_orientation_bound.rs` (2 tests) | met |
| #160 | the file-watch test does not depend on free CPU | test asserts `source`; fixture creates files until answered | `files.rs::should_report_a_created_file_before_the_next_poll_would_have_come` | met |
| #162 | a failing `cargo test` leaves no child; the follower test asserts its death | `OwnedChild`, `OwnedTree`, converted spawn sites; ADR-0892 | testkit `should_leave_no_process_behind_when_a_test_holding_one_panics` (+ session variant); `adapters.rs` follower test | met |
| #164 | the job test polls under a watchdog | `jobs_native.rs` | the test, 20/20 at load 14–24 and under extra load | met |
| #165 | both `near` counts from one run | `spatial_topology.rs` | the test, 10/10 under container churn | met |
| #166 | Profile L answers in 30 s at load 25, or names its machine | load-aware verdict with a declared skip; ADR-0881 | `spatial_first_output.rs::should_answer_or_refuse_within_the_interactive_budget_on_the_profile_l_fixture` | met (names the machine) |
| #169 | a benchmark under load reports that, not a regression | `Comparison::LoadedEnvironment`, recorded load; ADR-0904 | `perf.rs::should_report_a_loaded_machine_rather_than_a_regression_when_the_run_was_under_load`; a real run at load 4.6–6.2 printed "not adjudicated" | met |
| #185 | two concurrent acceptance runs in two worktrees each report their own results | per-checkout tag, shared/exclusive `flock`; ADR-0901 | `harness.rs` (2 tests); a real two-worktree run, both green | met |
| #188 | no `exec` of a file the test process holds open for writing | `executable_script`/`executable_copy` through a separate process; ADR-0891 | `harness.rs` stress tests (200 rounds each), 0 `ETXTBSY` | met |
| #189 | case 152 states what it does when the machine cannot meet the budget | same-machine, same-run reference; ADR-0882 | case 152 in CI and under added load | met |
| #196 | the documented sub-branch form can be created | AGENTS.md §12.1 `implementation-<slug>`, `ci.yml` trigger; ADR-0900 | `harness.rs::should_document_a_sub_branch_form_git_can_create_and_ci_runs_on` | met |
| #204 | an overrun ends the child's process tree | `ono_testkit::kill_tree` in `Shell` and `run_bounded`; ADR-0892 | `should_kill_what_the_program_started_when_a_run_exceeds_its_budget`, `should_kill_the_jobs_the_shell_started_when_a_bounded_run_is_cut_off` | met |
| #215 | three spatial tests stop comparing observations the host can change | `spatial_map.rs`, `spatial_topology.rs`; ADR-0880 | the three tests, 20/20 and under load | met |
| #218 | know where the release build's minutes go | measured from CI logs and `--timings`; `release-tool` profile; timing report per push; ADR-0865 | ADR-0865's figures; `release-build-timings` artifact | met |

Verification of the integrated tree, before the release commit: `scripts/gate.sh --static` green;
`cargo test --workspace --all-features --no-fail-fast` 576 binaries, 7,220 tests passed, 0 failed,
0 ignored; every CI acceptance group, the core-build job and the packaging job green. The run
record is `docs/runs/v0.6.2-2026-09-24.md`.

## Install

The `.deb` and `.rpm` for both architectures are on the release page:

```bash
sudo apt install ./ono_0.6.2_amd64.deb          # or ono_0.6.2_arm64.deb
sudo dnf install ./ono-0.6.2-1.x86_64.rpm       # or ono-0.6.2-1.aarch64.rpm

chsh -s /usr/bin/ono                             # make it your login shell
```

Verify before you install — the sequence is in `docs/reference/release-verification.md` and in the
README, and it is the one `scripts/verify-release.sh` runs, byte for byte:

```bash
sha256sum -c SHA256SUMS --ignore-missing
cosign verify-blob SHA256SUMS --signature SHA256SUMS.sig --certificate SHA256SUMS.pem \
  --certificate-identity-regexp '^https://github\.com/godspeed-you/ono-sendai/' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com
```

---
